import { describe, expect, it } from 'vitest'; import { extractAudio } from './scoreAgent.js'; import { buildCorrelationQuery, recommendationFromRows } from './retentionAgent.js';
import { isRetryable } from './server.js';
describe('agent integrations', () => { it('handles Lyria 3 output', () => expect(extractAudio({ outputs: [{ type: 'audio', data: Buffer.from('audio').toString('base64'), mime_type: 'audio/mpeg' }] }).bytes.toString()).toBe('audio')); it('builds a normalized read query', () => { const query = buildCorrelationQuery('owner', 100); expect(query).toContain('position_ratio'); expect(query).not.toContain('INSERT'); }); it('labels inference without events', () => expect(recommendationFromRows([{ position_ratio: .42, target_seconds: 42, severity_percent: 18, supporting_video_ids: ['a','b'], video_id: 'a', title: 'A', duration_seconds: 100, position_seconds: 42, nearby_events: [] }]).inferredCause).toContain('cannot be inferred')); it('retries transient service failures but not invalid configuration', () => { expect(isRetryable(new Error('fetch failed: ECONNRESET'))).toBe(true); expect(isRetryable(new Error('Lyria request failed with HTTP 503'))).toBe(true); expect(isRetryable(new Error('CLICKHOUSE_MCP_URL is required'))).toBe(false); expect(isRetryable(new Error('Gemini project id mismatch'))).toBe(false); }); });
