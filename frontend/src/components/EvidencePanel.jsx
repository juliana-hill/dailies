import React from 'react';

export default function EvidencePanel({ insight, fixtureMode, open, onToggle }) { return <section className="evidence-panel"><button className="evidence-toggle" onClick={onToggle} aria-expanded={open}><span><span className="eyebrow">ClickHouse MCP evidence</span><strong>How this signal was found</strong></span><span aria-hidden="true">{open ? '−' : '+'}</span></button>{open && <div className="evidence-body"><p>Normalized positions align videos of different lengths before converting each position back to seconds.{fixtureMode && ' These rows are explicit fixture data.'}</p><div className="query-box"><span>Read-only MCP query path</span><code>retention_curve_points → cut_events → normalized drop-off window</code></div><div className="evidence-table">{insight.evidence.map((item) => <article key={item.videoId}><div><strong>{item.title}</strong><small>{item.videoId} · {Math.round(item.positionRatio * 100)}% / {item.positionSeconds.toFixed(1)}s</small></div><span>−{item.dropPercent.toFixed(1)}%</span><p>{item.nearbyEvents.length ? item.nearbyEvents.join(' · ') : 'No nearby event data'}</p></article>)}</div></div>}</section>; }
