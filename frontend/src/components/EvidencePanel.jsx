import React from 'react';

export default function EvidencePanel({ insight, open, onToggle }) { return <section className="evidence-panel"><button className="evidence-toggle" onClick={onToggle} aria-expanded={open}><span><span className="eyebrow">ClickHouse MCP evidence</span><strong>How this signal was found</strong></span><span aria-hidden="true">{open ? '−' : '+'}</span></button>{open && <div className="evidence-body"><p>The normalized position aligns videos of different lengths before converting it back into the timestamp of each cut. These rows are fixture data for the frontend demo.</p><div className="query-box"><span>Read-only query path</span><code>retention_curve_points → cut_events → normalized drop-off window</code></div><div className="evidence-table">{insight.evidence.map((item) => <article key={item.videoId}><div><strong>{item.title}</strong><small>{item.videoId} · {Math.round(item.positionRatio * 100)}% / 00:{String(item.positionSeconds).padStart(2, '0')}</small></div><span>−{item.dropPercent}%</span><p>{item.nearbyEvents.join(' · ')}</p></article>)}</div></div>}</section>; }
