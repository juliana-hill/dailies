import React from 'react';

export default function RecommendationCard({ insight }) { return <section className="recommendation-card"><div className="recommendation-header"><p className="eyebrow">Your next cut</p><span className="confidence">{insight.confidence} evidence</span></div><h2>{insight.recommendationText}</h2><div className="time-range"><strong>00:29–00:36</strong><span>in your current cut</span></div><div className="recommendation-grid"><div><span>Observed</span><p>{insight.observedEvidence}</p></div><div><span>Inference, not certainty</span><p>{insight.inferredCause}</p></div></div><div className="action-box"><span>Suggested action</span><strong>{insight.suggestedAction}</strong></div><p className="supporting-ids">Supporting videos: {insight.supportingVideoIds.map((id) => <code key={id}>{id}</code>)}</p></section>; }
