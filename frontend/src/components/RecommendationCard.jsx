import React from 'react';

export default function RecommendationCard({ insight }) { const hasHistory = insight.supportingVideoIds.length > 0; return <section className="recommendation-card"><div className="recommendation-header"><p className="eyebrow">Your next cut</p><span className="confidence">{insight.confidence} evidence</span></div><h2>{insight.recommendationText}</h2>{hasHistory && <div className="time-range"><strong>{formatTime(insight.dropOffSeconds)}</strong><span>in your current cut</span></div>}<div className="recommendation-grid"><div><span>Observed</span><p>{insight.observedEvidence}</p></div><div><span>Inference, not certainty</span><p>{insight.inferredCause}</p></div></div><div className="action-box"><span>Suggested action</span><strong>{insight.suggestedAction}</strong></div>{hasHistory && <p className="supporting-ids">Supporting videos: {insight.supportingVideoIds.map((id) => <code key={id}>{id}</code>)}</p>}</section>; }
const formatTime = (seconds) => `${String(Math.floor(seconds / 60)).padStart(2, '0')}:${String(Math.round(seconds % 60)).padStart(2, '0')}`;
