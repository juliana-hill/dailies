import React from 'react';
import SceneTimeline from './SceneTimeline';
export default function AnalysisPanel({ result, selectedScene, selectedSceneId, onSceneSelect }) { return <section className="analysis-panel report-panel"><div className="section-heading"><div><p className="eyebrow">01 · Analysis</p><h2>The movement of this cut.</h2></div><span className="demo-badge">Fixture analysis</span></div><div className="analysis-layout"><div><SceneTimeline scenes={result.scenes} activeId={selectedSceneId} onSelect={onSceneSelect} /></div><aside className="scene-notes"><span className="eyebrow">At {String(selectedScene.startSeconds).padStart(2, '0')} seconds</span><div className="tag-row"><span className="tag violet">{selectedScene.mood}</span><span className="tag">{Math.round(selectedScene.energy * 100)}% energy</span></div><p className="transcript">“{selectedScene.transcript}”</p>{selectedScene.pacingFlags.length ? <div className="flag-list"><span>Editing flags</span>{selectedScene.pacingFlags.map((flag) => <strong key={flag}>{flag}</strong>)}</div> : <p className="clear-state">No pacing flags in this moment.</p>}</aside></div></section>; }
