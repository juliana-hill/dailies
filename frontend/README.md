# Dailies frontend

React + hooks + Redux Toolkit client for the Dailies creator workflow.

The marketing landing page remains in `public/` as a static, framework-independent surface. This package will own the interactive application experience.
