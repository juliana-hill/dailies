{
  "name": "dailies",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "api",
    "shared",
    "agents",
    "ingestion"
  ],
  "scripts": {
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:api": "npm --workspace api run dev",
    "dev:agents": "npm --workspace agents run dev",
    "dev:ingestion": "npm --workspace ingestion run dev",
    "build": "npm --workspace shared run build && npm --workspace api run build && npm --workspace agents run build && npm --workspace ingestion run build && npm --workspace frontend run build",
    "test": "npm --workspace shared run build && npm run test --workspaces --if-present",
    "audit:ai-policy": "node scripts/audit-ai-dependencies.mjs",
    "up": "docker compose up -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "compose:build": "docker compose build",
    "restart": "docker compose down -v && docker compose build --no-cache && docker compose up -d"
  },
  "engines": {
    "node": ">=20"
  }
}
